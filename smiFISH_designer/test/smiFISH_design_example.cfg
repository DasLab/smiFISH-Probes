# You must fill in values in the required section!
[REQUIRED]
TargetRNAName = MBP
TargetRNASequence = 

FLAPName = X
FLAPSequence = CCTCCTAAGTTTCGAGCTGGACTCAGTG

OutputFolder = ../output


# These are optional, but recommended to leave as default
[OPTIONAL]
# Minimumm length of desired probes (without FLAP sequence)
ProbeMin = 20

# Maximum length of desired probes (without FLAP sequence)
ProbeMax = 30

# Maximum length of acceptable probe length. Based on the company you are ordering probes with (with FLAP sequence)
AcceptableProbeMax = 60